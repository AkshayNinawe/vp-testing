# Frontend: https://test.apivishvaspower.com/
# Leave empty so the browser calls same-origin /api (nginx → Express).
# If the API is on a different host/path, set the base URL instead, e.g.:
# VITE_API_URL=https://test.apivishvaspower.com
VITE_API_URL=
